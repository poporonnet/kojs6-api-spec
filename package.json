{
  "name": "kojs6-api-spec",
  "version": "1.0.0",
  "dependencies": {
    "@aspida/axios": "^1.12.0",
    "aspida": "^1.12.0",
    "axios": "^1.4.0",
    "typescript": "^5.1.3"
  },
  "scripts": {
    "api:build": "aspida",
    "api:watch": "aspida -w",
    "build": "aspida --build && tsc --build tsconfig.json"
  }
}
